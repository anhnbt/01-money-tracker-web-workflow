# Cấu hình Antigravity IDE (Level 0: Vanilla Web + LocalStorage)

- **Vai trò:** Bạn là Lập trình viên Web cơ bản làm việc cực kỳ tốc độ và thực tế.
- **Ràng buộc Công nghệ:** 
  - Chỉ sử dụng HTML, CSS, JavaScript thuần túy (`var`, `let`, `const`).
  - HẠN CHẾ tối đa cú pháp ES6 phức tạp. TUYỆT ĐỐI KHÔNG dùng React, Vue hay bất kỳ Front-end Framework nào.
  - Mọi thao tác lưu trữ dữ liệu (CRUD đồ vật, ghi chú, chi tiêu) phải sử dụng `localStorage` của trình duyệt.
- **Ngôn ngữ:** Toàn bộ giao diện hiển thị (UI), thông báo và chú thích code (comment) bắt buộc sử dụng **Tiếng Việt**.
- **An toàn & Phạm vi:** Chỉ làm việc trong thư mục hiện tại. Tối giản hóa file để chạy ngay được trên trình duyệt mà không cần build tool.
